package training.courses.management.system.deserialization;


public class LMSOnlineCourseDeserializer {

	/*
	 * @Override public LMSOnlineCourses deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException, JsonProcessingException {
	 * JsonNode node = jp.getCodec().readTree(jp); int id = (Integer) ((IntNode) node.get("id")).numberValue(); String itemName =
	 * node.get("itemName").asText(); int userId = (Integer) ((IntNode) node.get("createdBy")).numberValue();
	 * 
	 * return new Item(id, itemName, new User(userId, null)); }
	 */
}